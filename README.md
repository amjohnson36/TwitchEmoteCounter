# twitch_emote_counter

This bot tracks the use of twitch emotes in given chats over time. To use first run python3 bot.py and enter the chat room and how many minutes you want it to run for.

This data will be stored in a data.json file. To graph it, make sure you have a /graphs directory then run python3 plot.py
